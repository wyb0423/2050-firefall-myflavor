interface:
  display_name: "Victoria 3 Mod Development"
  short_description: "Build and debug compatible Victoria 3 mods"
  default_prompt: "Use $victoria3-mod-development to detect the host OS, select the matching local workflow, and research, implement, and verify this Victoria 3 mod change. On macOS, follow the skill's Darwin path and shell guidance first."
