# USA《一个更大的共同体》生命周期修复

用户已确认：美国大陆本土全部省份直接归 USA 后正式结束日志；激活期间地区整合速度 +25%，冷却期间有效，完成或身份失效撤除。

## 范围与终点

- 沿用现有49个大陆州区域：48州及哥伦比亚特区，不含阿拉斯加、夏威夷和波多黎各。由原省份生成器输出完整区域归属条件，与2,192个省份的既有目标表同源。
- 原生 `owns_entire_state_region` 要求完整直接所有权；占领、属国持有、暂时没有可执行近邻不构成统一完成。
- USA、非革命身份、全部本土归属、状态0且无待答复提案才能完成。最后一轮接收先结算，不以冷却阻止最终完成。
- `ffpa_usa_union_complete_v1` 是新增永久完成标记，阻止日志补挂；既有五年奖励保留，不发额外终局奖励。

## 交互与收尾

- 轮换按钮及执行入口共用候选判断：零候选禁用，唯一候选已选中时禁用，尚未选择时允许选取。候选查询限定既有49区。
- 提案按钮、事件可用条件和执行保护共享当前身份、目标、和平、冷却条件；每次提案更新旧弹窗票据。
- 增加继续接收会议入口及事件暂缓选项；一个月重开间隔，重开使旧票据失效。接收按钮和事件选项实时检查登记省份所有权，执行时仍重新计数防止重复或错误奖励。
- 月度检查记录期限内完整取得的目标，即使仍在战争中也进入待接收；仅接收奖励等待和平。期限已过而仍处于活动状态则锁定取消，不能再补足领土获得成功。待接收期间失去登记领土则恢复活动检查。
- 已有状态0/1/2/3和省份快照不改含义。旧档的状态2保留待结算资格；新完成标记及重开间隔缺省为未设置。

## 激活效果与兼容

- `ffpa_usa_union_integration_v1` 仅给符合条件且日志激活的 USA，国家修正为地区整合速度 +25%。前身共享按钮修复，不能获得 USA 常驻效果或终局。
- 复用日志初始化、原月度恢复包装及失效／完成回调，幂等补齐和清理，不新增调度、不续期有限奖励。
- 不修改 Core Balance、Firefall、Tech & Res 或 CMF 的定义，不改变现有宣称来源清理规则。

## 实施与验收

1. 修改共享 trigger、按钮和事件保护，生成有界实时所有权判断及正式终点。
2. 接通常驻效果、待接收恢复和完成标记，同步双语状态、目标、按钮说明与维护文档。
3. 扩展现有统一检查，运行北美激活、工程、地图预检、USA政治／宪章／经济、展示、本地化及生成一致性检查。
4. 检查最终本地定义来源、元数据、编码、重复键及 Git diff；完成后本地提交。

静态测试执行真实脚本的有限子集，地图所有权、引擎回调等由测试事实提供；不证明引擎加载、UI、存读档或实际AI。遵守项目约束，不启动游戏。本机保存的加载清单未启用本任务加载栈，不能用于实机结论。
