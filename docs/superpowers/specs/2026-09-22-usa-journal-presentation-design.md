# USA 日志表现优化

2026-09-22 用户确认实施：富文本分层、原生进度条与少量 CMF 只读组件。

- 《重新缔结联邦》：四项议题阶段格、已定制度、合法性和官僚条件、独立宪章状态。
- 《大陆市场》：人口覆盖、设施州数、当前状态和拨款；保留原生十二个月进度条。
- 《规模的力量》：发展路线、固定规模目标、行业与州分布、两项行业结构条件、拨款；保留原生十八个月进度条。
- 《一个更大的共同体》：仅 USA 增加目标、状态、登记省份进度与期限规则；前身国家仍读取完整原描述。

保留 CMF 原生日志顺序：操作按钮、条件组件、既有连续月份条、风味正文和成果。新增组件挂在 `custom_widget_container_3`，不覆盖公共窗口，不改其他国家的组件。阶段格复用 CMF 1.65.0 的 `com_journal_entry_enactment_phase`；经济时间条仍由原生容器渲染。

正文保留原意并拆段；标题与数值强调，收益与达标用绿色，等待与未满足用黄色，实际签署错误用红色，状态始终带文字。分类和统计口径放到对应悬浮提示；门槛、持续期、归零规则和实际拨款直接显示。

所有显示读取原有条件、脚本值和变量。缺失旧档计数显示零，目标对象先检查存在性；不新增存档变量、缓存、调度、奖励或机制。条件行与原始机制的对应由 `tests/check_usa_journal_presentation.py` 验证。

验证：本地化、CMF 挂载/资源、新显示条件及分支、原有政治/经济/宪章/统一检查；对比修改前后 JE 去掉 widget 后完全一致。游戏内换行、阶段格、悬浮作用域、缩放及读档仍须在完整 Firefall 加载集验证。当前启用集不包含该环境，不自动切换启动器设置。

## 事件文案与图标后续调整

用户追加确认：15 场 USA 事件正文简化分段，选项只表述行动，精确效果移入悬浮提示。普通修正仍使用原生提示；动态拨款费用及两场复合结算使用自定义提示，原效果放入 `hidden_effect`，保留有效性检查与执行顺序。签署前保留三组法律对照，期限与费用放入草案、延期和签署选项提示。

两条经济日志移除正文奖励清单，提供可悬浮的成果摘要；进度、条件、实际拨款继续可见。事件与日志复用天平、选举、工业、贸易、地图图标；事件插图按议事、机械、列车、签约区分，均为本地原版资源。

执行 `tests/check_usa_event_presentation.py --game-root GAME`，验证双语结构、奖励分层、提示挂载及图标/影片存在性，并继续运行政治、经济、宪章与 CMF 检查。静态解释器按正常效果执行 `hidden_effect`，避免隐藏包装跳过行为测试。引擎悬浮渲染与画面构图仍待实机验证。
